class Magic extends Sprite {
  constructor(config) {
    config.spriteType = 'magic';
    super(config);
  }

  createDefaultHitbox() {

    this.init();
  }

  init() {  }

  delete() {

  }

  update(state) {  }

  render() {

  }
}